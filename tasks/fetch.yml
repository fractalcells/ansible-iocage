---
- name: "Check if release {{ release }} exists"
  block:
    - name: "Check if release {{ release }} exists"
      iocage:
        state: fetched
        release: "{{ release }}"
      register: result
    - debug:
        var: result
      when: debug|bool
  rescue:
    - debug:
        var: ansible_failed_result
